angular.module('MyApp')
	.controller('NavCtrl', ['$scope', function ($scope) {
		
		// CHECK WIDTH OF SCREEN
		/*
		$scope.current_width = screen.width;

		$scope.not_presentation = true;

		if ($scope.current_width >= 2560) {
			$scope.not_imac_presentation = false;
		}
		*/
	}])